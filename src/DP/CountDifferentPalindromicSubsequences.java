package DP;

public class CountDifferentPalindromicSubsequences {
    public int countPalindromicSubsequences(String s) {
        int MOD = (int) (1e9+7);
        // len = s.length();
        // n = length of the Palindrome
        // n = 1;  s.substring(i,j)  == all the different
        // n = 2;  i == j;
        // n = 3;  k != i and j;/
        // n = 4;  i,j,2 * sum, for i+k,j-k,2;
        // n = 5;

        return 0;

    }
}
