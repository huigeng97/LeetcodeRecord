package DP2;

import java.util.*;

public class LongestStringChain1048 {
    public int longestStrChain(String[] words) {

//        Arrays.sort(words, (a,b) -> a.length() - b.length());
//
//        int start = 0;
//        Map<Integer, List<String>> map = new HashMap<>();
//         for (String word : words) {
//            if ( map.containsKey(word.length() - 1)) {
//                return word.length() - start;
//            } else {
//                  if (isValid (word, map.get(word.length() - 1))) {
//                        map.getOrDefault(word.length(), new ArrayList<String>()).add(word);
//                  }
//            }
//         }
//         return
        return 0;
    }
}
